# Klaus — instrucciones para agentes

Este archivo es sólo un puntero, para que todos los agentes (Claude Code, Codex, Hermes Agent, OpenCode, OpenClaw) lean las mismas reglas.

- Este repo no tiene `CLAUDE.md` todavía: las reglas y el contexto están en la ficha `proyectos/Klaus.md` del vault.
- **El contexto general** (estado de todos los proyectos, lecciones transversales, credenciales, historial) está en el vault de Obsidian, el cerebro compartido: `C:\Users\berna\OneDrive\Documentos\Obsidian\Berna Notebook\` (WSL: `/mnt/c/Users/berna/OneDrive/Documentos/Obsidian/Berna Notebook/`). Empezá por `_compartido-agentes.md` y la ficha `proyectos/Klaus.md`.

Git en este repo se opera con el de Windows (desde WSL: `powershell.exe -Command "git -C '<ruta>' ..."`). Antes de tocar nada, `git pull` y `git log --oneline -5`: otras máquinas y otros agentes también commitean acá.
